# $TITLE

## Composition Note

This document describes the composed runtime state after applying `$SCENARIO_FILE` on top of the default system.

$COMPOSITION_NOTE

## Overview

$OVERVIEW

### Node

$NODE_SECTION

### Edge

$EDGE_SECTION

## Usage

```bash
python3 main.py --scenario $SCENARIO_FILE --no-feedback --cycles 1
```
